/// <reference types="jest" />
// Make @testing-library/jest-dom matchers (e.g., toBeInTheDocument) available to TypeScript.
// Placing this under `src/` ensures the IDE picks up the global type augmentations.
import '@testing-library/jest-dom'

